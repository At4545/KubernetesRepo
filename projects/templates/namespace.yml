#@ load("@ytt:data", "data")
---
apiVersion: v1
kind: Namespace
metadata:
  name: #@ data.values.namespace.name
  label: #@ data.values.namespace.name
  annotations: #@ data.values.annotations.annotations

